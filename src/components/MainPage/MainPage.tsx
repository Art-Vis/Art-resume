import { FC } from 'react';
import AppRoutes from '../AppRoutes/AppRoutes';

const MainPage: FC = () => {
	return (
		<main className='main'>
			<AppRoutes />
		</main>
	);
};

export default MainPage;
